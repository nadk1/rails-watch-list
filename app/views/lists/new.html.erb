<div class="container">
  <h1>Add a list</h1>

  <%= simple_form_for @list do |f| %>
    <%= f.input :name, label: "Name" %>
    <%= f.button :submit, value: "Create list" %>
  <% end %>

  <%= link_to "Back", lists_path, class: "btn btn-gradient" %>
</div>
